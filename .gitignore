# =============================================================================
# eng:: — Git ignore (FASE 1)
# =============================================================================

# Diretórios de build (presets geram build/<preset>) — ANCORADO na
# raiz: engine/build/ é MÓDULO-FONTE (FASE 12) e DEVE ser rastreado.
/build/
/.build/
/out/
/cmake-build-*/

# Projetos de jogo: cache derivado e estado local (FASE 3, ADR-032)
cache/
.goni/

# Workspaces descartáveis dos testes do editor (EditorHost::create no cwd)
.editor-test-ws-*/

# CMake
CMakeUserPresets.json
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
compile_commands.json
_deps/

# Artefatos compilados
*.o
*.obj
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll
*.exe
*.pdb

# FetchContent / dependências baixadas
third_party/src/
third_party/build/

# IDE / editores
.idea/
.vscode/
.cache/
*.swp
*.swo
.vs/

# Sistema operacional
.DS_Store
Thumbs.db

# Secrets — NUNCA commitar (guardrail §15.1)
.env
.env.*
*.token
*.key
secrets/

# ferramentas de DEV compiladas (scripts/*.cpp ficam, binários não)
scripts/nidbg
scripts/nidbg*

# Workspaces do teste host-sprite (evolução P0-3 — criados no cwd)
sprite_host_test_*/
p31_*.log

# Android (Gradle/IDE)
android/local.properties
android/.gradle/
android/build/
android/*/build/
android/app/.cxx/
